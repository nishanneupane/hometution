import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"
import { Toaster } from "@/components/ui/sonner"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "HR Home Tuition - Quality Education at Home",
  description:
    "Connect with qualified tutors for personalized home tuition services. Expert teachers for all subjects and grades across Nepal.",
  keywords: "home tuition, tutors, Nepal, education, learning, private tutoring, Kathmandu",
  authors: [{ name: "HR Home Tuition" }],
  creator: "HR Home Tuition",
  publisher: "HR Home Tuition",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  )
}
